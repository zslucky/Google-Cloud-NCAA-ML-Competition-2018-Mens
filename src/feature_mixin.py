import pandas as pd

def merge_teams_score_to_games(df_sub, df_season_teams_scores):
    """
        Merge teams score features to submission file for both played teams
    """

    df = pd.DataFrame({})



    return df